**What broke, and how did you observe it?**

**How is it tested?**

- [ ] A test that fails before this change and passes after
- [ ] I broke the fix on purpose and confirmed the test went red
- [ ] `tests/test-constraints.sh` passes
- [ ] `tests/test-provision.sh` passes (0 skipped)
- [ ] `shellcheck` is clean

If any box is unticked, say why — there are good reasons, but they should be
stated rather than assumed.
